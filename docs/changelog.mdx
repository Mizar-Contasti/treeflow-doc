---
sidebar_position: 12
---

# 📜 Historial de Cambios

> Para una versión más detallada del historial de cambios con todas las características y mejoras técnicas (incluyendo las arquitecturas internas de IA y Componentes Web), consulta el [Historial de Cambios Detallado](/docs/Misc/HistorialDetallado).

## 🎉 Versiones Recientes (2026)

<details open>
<summary><strong>V-0.9.0 (Septiembre 2026) - Entidades Compuestas con Alias, Pedidos de Varias Líneas y Slot Filling que no Atrapa</strong></summary>

### 🧩 Hojas Compuestas: Filas de Combinación y Alias de Parámetro
> - **Definición por Filas**: Una hoja compuesta se define ahora con varias filas, igual que las frases de una intención. Cada fila es una forma válida de decir lo mismo —el pedido en otro orden, o con una parte de menos— y gana la que rellena más huecos.
> - **Alias con Dos Puntos**: `@Productos:producto` guarda la hoja `Productos` bajo el nombre de parámetro `producto`. Sin los dos puntos, el parámetro se llama igual que la hoja.
> - **Palabras Literales en las Filas**: Una fila admite texto fijo entre los huecos (`@categoria:cat de @sabor:sab`); si está escrito, tiene que aparecer en la frase.
> - **Resaltado del Token Completo**: En el editor, el resaltado cubre `@Productos:producto` entero. Antes el alias quedaba fuera y parecía texto suelto de la frase.

### 📋 Un Parámetro Puede Traer Varios Valores
> - **Ocurrencias Múltiples**: Si la misma hoja aparece dos veces en la frase, el parámetro guarda **las dos**, en orden. Hasta ahora la segunda pisaba a la primera y desaparecía sin aviso.
> - **Acceso como Lista**: `{{$producto[0]}}`, `{{$producto[1]}}` y `{{len($producto)}}`. Un parámetro que vino una sola vez se comporta como una lista de uno, así que `[0]` funciona siempre.
> - **Declararlo Plural**: Escribir la hoja dos veces en la frase de entrenamiento le dice al árbol que ese parámetro puede venir repetido.

### 🧾 Pedidos de Varias Líneas
> - **Enumeración**: *"dame 1 empanada de pollo y 2 arepas de carne"* se anota y se confirma como las dos cosas. Antes se confirmaba sólo la primera y la segunda se perdía en silencio.
> - **Preguntar por Cada Línea**: Con dos renglones incompletos, el árbol pregunta por cada uno y nombra de cuál habla: *"¿De qué sabor quieres las 3 arepas?"*. Antes, en cuanto una línea traía el dato, el requisito se daba por cumplido y el pedido se cerraba incompleto.
> - **Sumar frente a Corregir**: *"y además tres arepas"* añade; *"no espera, mejor de carne"* corrige la última y hereda lo que la frase no menciona. Se exige un marcador explícito para sumar, porque anotar comida que nadie pidió es peor que corregir de más.
> - **El Pedido Termina al Cerrarlo**: Cerrar o cancelar vacía el pedido. Antes el siguiente heredaba el anterior.

### 🎛️ Slot Filling que no Deja Atrapado al Usuario
> - **Interruptor "Termina una captura en curso"**: Nueva opción por nodo de intención. Cancelar o despedirse terminan la captura; una duda de paso, como preguntar el horario, sólo la pausa y después se retoma.
> - **Máximo de Intentos y Acción al Superarlo**: `Pasar al siguiente parámetro`, `Responder aunque falte el valor` o `Ir al nodo de contexto`. Si todos los obligatorios agotan sus intentos, el árbol abandona con un mensaje en vez de dar vueltas.
> - **No se Pregunta lo que no Tiene Sujeto**: Un enunciado que se apoya en otros parámetros no se pregunta si todos están vacíos. Se acabó el *"¿De qué sabor quieres?"* —de nada— y la cantidad de nada detrás.
> - **Recaptura**: Los parámetros marcados como recapturables pueden corregirse mientras se pregunta por otro.

### 🔤 Lenguaje de Plantillas
> - **`{{$variable}}` es la Sintaxis**: El `$` es obligatorio para leer un parámetro. También dentro de expresiones: `{{ if $numero > 1 ? "${producto}s" : $producto }}`.
> - **Comparadores**: `$numero > 1` y el resto de comparaciones funcionan sobre parámetros.
> - **`${var}` Dentro de Cadenas**: Interpolación dentro de un literal de texto.
> - **Un Parámetro Inexistente ya no Rompe el Mensaje**: Vale como vacío y sólo se queda en blanco esa parte, en lugar de tumbar la expresión entera y dejar la respuesta muda.
> - **Sin Conectores Huérfanos**: Un hueco vacío ya no deja *"¿Cuántas empanada de  te preparo?"*.

### 🎯 Detección: Cada Palabra Clave en su Intención
> - **Límites de Palabra**: Las frases de una hoja casan por palabras completas. Antes `"el bar"` casaba dentro de `"el BARrio"` y `"a m"` dentro de `"modificA MI pedido"`.
> - **Nombre Bien Escrito en las Respuestas**: El bot confirma con el nombre correcto, no con la errata del usuario: *"kiero 2 empanads de keso"* se anota como *"2 empanadas de queso"*.
> - **Un Trozo de Frase no Llena Dos Huecos**: En *"mañana a las 8 de la noche"*, el 8 es la hora y no además el número de personas.
> - **Palabras Clave Compartidas**: Varias intenciones que compartían vocabulario dejaban el reparto a la suerte; cada una tiene ahora su discriminador.

### 📦 Exportación
> - **"Incluir conversaciones" se Respeta**: La casilla ya funciona. Desactivada —por defecto— el archivo no arrastra el historial, que en un bot en producción son miles de turnos con datos de clientes.

### ⚡ Rendimiento del Editor
> - **Contextos Memorizados**: Los contextos globales creaban un objeto nuevo en cada render, así que re-renderizaban a todos sus consumidores aunque no cambiara ningún dato. Medido con 100 consumidores: de 100 renders por ciclo a 0.

</details>

<details>
<summary><strong>V-0.8.0 (Agosto 2026) - Presencia en Tiempo Real, Debug Visual, Dialog Builder y Autenticación Mejorada</strong></summary>

### 👥 Sistema de Presencia en Tiempo Real
> - **Cursores en Vivo (Live Cursors)**: Los administradores ven el cursor de cada colaborador en tiempo real sobre el canvas y el árbol conversacional, mediante WebSocket.
> - **Avatares de Usuarios Conectados**: Indicadores visuales en el Dashboard, Navbar, canvas (header de rama + badges por nodo), y en los editores de Intenciones y Entidades.
> - **Follow Mode**: Modo de seguimiento flotante — un admin puede sincronizar su vista con la de otro usuario conectado con un solo clic.
> - **Selección Resaltada**: Al seleccionar un nodo, los demás usuarios conectados ven un anillo de color único alrededor del elemento seleccionado.
> - **Restricción a Administradores**: La interacción en tiempo real, cursores y follow mode están restringidos exclusivamente a usuarios con rol `admin`.
> - **Endpoint de Presencia Global**: Nuevo endpoint `GET /trees/{tree_id}/presence/overview` que devuelve la lista de usuarios activos con su ubicación actual en el árbol.

### 🐛 Debug Visual en Canvas
> - **Trazado en Tiempo Real**: Durante las pruebas en la consola, los nodos visitados por el motor se resaltan visualmente en el canvas con un color diferenciado.
> - **Auto-Navegación de Ramas**: El canvas navega automáticamente hacia la rama activa durante la sesión de debug, manteniendo el foco en el flujo en ejecución.
> - **Conexiones Trazadas**: Las aristas (conexiones) entre nodos también se resaltan para visualizar el camino de decisión tomado por el motor.

### 🏗️ Dialog Builder — Nueva Pestaña de Consola
> - **Tercera Pestaña en la Consola**: Se agregó una pestaña dedicada *Dialog Builder* en la consola lateral del editor, junto a Chat y Debug.
> - **Historial de Chat como Burbujas**: La ventana de conversación fue rediseñada completamente con un estilo de historial de burbujas moderno, diferenciando visualmente turnos del usuario (naranja primario) y del bot.
> - **Debug Accordions Inline**: Los datos de debug (confidence score, entidades extraídas, rama ejecutada) se muestran como acordeones colapsables directamente dentro del historial, sin interrumpir el flujo de la conversación.
> - **Modo QA Restringido**: El toggle de nodo y el selector de rama están desactivados en modo consola para evitar modificaciones accidentales durante las pruebas.

### 🔐 Autenticación Mejorada
> - **Auto-Logout por Inactividad**: Cierre de sesión automático tras 1 hora sin actividad del usuario, con advertencia previa.
> - **Checkbox "Recordar Credenciales"**: Opción en el formulario de login para persistir las credenciales entre sesiones del navegador.

### 🌿 Duplicación de Ramas
> - **Acción de Duplicar Rama**: Nueva opción en la vista `/branches` (modo test/experto) para duplicar una rama completa con un prompt de nombre personalizado.

### 🔧 Correcciones de Motor (Regex y API)
> - **Estrategias de Matching (`utils.py`)**: Correcciones en los utilitarios de estrategias de matching regex del motor determinista.
> - **Endpoints de Conversaciones**: Alineación de los endpoints de conversaciones con el path canónico `/trees/{tree_id}/conversations` y alias `/api/trees/` en `app.py`.
> - **Fix TypeScript**: Eliminación de importaciones no utilizadas y corrección de tipos en componentes UI (`NavIcons`, `GeneralTab`, `ConsoleChatWindow`).

</details>

<details>
<summary><strong>V-0.7.0 (Julio 2026) - Multicanalidad Avanzada, Modales Estandarizados y Payloads Nativos por Plataforma</strong></summary>

### 🔌 Nuevas Integraciones y Reorganización de Canales (`backend/api/integrations/`)
> - **Slack App (Events API)**: Integración en tiempo real con Slack API y renderizado nativo **Block Kit** (`section`, `actions`, `button`, `image`).
> - **Discord Bot API**: Soporte para **Rich Embeds** y componentes `ActionRow` interactivos con respuesta de verificación ED25519 (`type=1`).
> - **ElevenLabs Voice AI**: Síntesis neuronal de voz multilingüe (`eleven_multilingual_v2`) con selector de voces.
> - **Avatares IA**: Generación de video-respuestas interactivas con avatares de HeyGen y D-ID.
> - **Estructura Limpia de Integraciones**: Routers modularizados en `backend/api/integrations/` y clientes API cliente en `frontend/src/api/integrations/`.

### 🎛️ Estandarización de Modales e Interfaz UI
> - **Tarjeta de Estado e Interruptor ON/OFF**: Reorganización de los 8 modales de integraciones con tarjeta visual de estado (`IntegrationStatusCard.tsx`) e interruptor ON/OFF interno para activar/desactivar conexiones sin salir del modal.
> - **Categorización de Integraciones (`IntegrationsView.tsx`)**: Etiquetas estandarizadas: 🟢 **Estable** (Telegram, Web), 🟡 **Inestable** (WhatsApp, Facebook, Slack, Email, Instagram, Discord, ElevenLabs, Avatares IA) y ⚪ **Pendiente implementar** (SMS, BYOC).

### 💬 Bloques Visuales y Payloads Nativos por Plataforma
> - **Pestañas Dinámicas por Canal**: En el editor de respuestas (`ResponsesSection.tsx`), cada integración activa cuenta con su pestaña dedicada para crear respuestas y bloques enriquecidos a la medida del canal.
> - **Traductor Nativo Multiplataforma (`native_rich_messages.py`)**: Traducción inteligente de bloques visuales de Treeflow a estructuras nativas oficiales de WhatsApp (Botones/Listas interactivas), Facebook (Generic Templates/Quick Replies), Slack (Block Kit), Discord (Rich Embeds/Buttons) y Resend Email (HTML Responsivo con CTAs).
</details>

<details>
<summary><strong>V-0.6.0 (Julio 2026) - AI Builder Agent, Seguridad Multitenant y Optimización de Producción</strong></summary>

### 🤖 Treeflow AI Builder Agent
> - **Consola Asistida de IA**: Consola inteligente capaz de crear, consultar y modificar ramas, nodos, intenciones, entidades, plantillas de mensajes, usuarios y fertilizantes en tiempo real.
> - **Protocolo Anti-Duplicados de 2 Capas**: Regla obligatoria en System Prompt y validaciones backend en `create_branch`, `create_leaf`, `create_intent`, `create_entity`, `create_message_template` y `create_fertilizer` que impiden crear recursos duplicados por nombre.
> - **Savepoints Transaccionales (`db.begin_nested()`)**: Ejecución segura de herramientas con rollback automático en caso de error para proteger la integridad del árbol.

### 🛡️ Seguridad e Aislamiento Multitenant
> - **Verificación de Propiedad (`verify_tree_ownership`)**: Validación de pertenencia del árbol al workspace (`tree.workspace_id == current_user.workspace_id`) en `/chat`, `/confirm` y `/sessions`.
> - **Inmunidad a Prompt Injection / Alucinaciones**: Sobrescritura forzosa a nivel de backend de los parámetros `tree_id` y `workspace_id` al ejecutar las herramientas del agente.
> - **Filtros Joins de BD y Cabeceras HTTP**: Aislamiento estricto por `tree_id` en PostgreSQL e inyección automática de cabeceras de seguridad (`X-Content-Type-Options`, `X-Frame-Options`, `X-XSS-Protection`, `Referrer-Policy`).

### ⚡ Rendimiento, Rate Limiting y Producción
> - **Rate Limiting (120 req/min)**: Protección mediante ventana deslizante en el Motor Determinista (`/api/process-message`) y Webhooks de Canales (`/api/webhooks/telegram/{tree_id}`) contra ataques de flooding (DoS).
> - **Compresión GZip (`GZipMiddleware`)**: Reducción de tamaño de transferencia del 80-90% para payloads > 1KB (aceleración visual de canvas y listados).
> - **Indexación B-Tree**: Índices en `Leaf.branch_id` y `Branch.tree_id` en PostgreSQL para consultas sub-milisegundo.
</details>

<details>
<summary><strong>V-0.5.0 (Junio 2026) - Plantillas, Telegram y Bloques Ricos</strong></summary>

### 🔌 Integraciones y Webhooks Avanzados
> - **Telegram Oficial**: Conexión nativa con Telegram, renderizado de 14 bloques ricos (carruseles, botones, multimedia) con pruebas End-to-End.
> - **Webhooks de Terceros**: Soporte y estabilidad de webhooks (`Fertilizantes`).

### 🤖 Generación y Plantillas
> - **Modelos Industriales**: Refactorización profunda de plantillas.
> - **Reforestación Compleja**: Modificaciones y mejoras al parseo natural y resaltado en el editor.
> - **Soporte de Entidades Relativas**: Captura de fechas dinámicas y nombres propios complejos en plantillas.
</details>

<details>
<summary><strong>V-0.4.2 (Mayo 2026) - Estabilidad UI y Pruebas</strong></summary>

### 🎨 Experiencia y QA
> - **Responsividad Total**: Ajustes completos para que TreeFlow funcione en todos los tamaños de pantalla.
> - **Agentes Predeterminados**: Adición de bots y agentes pre-cargados para iniciar rápido.
> - **Testing Automatizado**: Integración con Playwright para pruebas E2E robustas.
</details>

<details>
<summary><strong>V-0.4.1 (Abril 2026) - Estabilización de Motor</strong></summary>

### 🛠️ Mejoras de Core
> - Fix global para guardado de ramas y estado.
> - Corrección en la habilitación e inhabilitación de parámetros (slot-filling).
> - Traducciones finales de la interfaz.
</details>

<details>
<summary><strong>V-0.4.0 (Marzo 2026) - IA Generativa Aplicada e Infraestructura</strong></summary>

### 🤖 GenAI y UI
> - **Herramientas de IA (Tools)**: Motor generativo capaz de usar herramientas externas en tiempo real.
> - **Modo Principiante**: Introducción de tutoriales interactivos y modos simplificados.

### 🚀 Despliegue y DevOps
> - **Entorno Staging**: Configuración de URLs dinámicas a través de Cloudflared.
> - **CORS Dinámico**: Solución para convivencia de Frontend, Backend y Grafana en puertos separados.
</details>

<details>
<summary><strong>V-0.3.2 (Febrero 2026) - Arquitectura y Seguridad</strong></summary>

### 🛡️ Seguridad
> - Corrección profunda a políticas de inicio de sesión (Login) y propiedades de seguridad.
> - Arquitectura técnica documentada.
</details>

<details>
<summary><strong>V-0.3.1 (Enero 2026) - Slotfilling y Variables Dinámicas</strong></summary>

### 💬 Respuestas Inteligentes
> - Soporte para inyectar y utilizar variables de parámetros directamente en los mensajes de texto (`{variable}`).
> - Multi-parámetros (Slotfilling) optimizado para múltiples entidades en una misma intención.
</details>


## 📜 Histórico Base (2025)

<details>
<summary><strong>V-0.3.0 (Diciembre 2025) - El Lienzo Conversacional (Canvas)</strong></summary>

### 🎨 Canvas Engine
> - **Lanzamiento del Editor Visual (Canvas)**: Interfaz gráfica de arrastrar y soltar funcional con el Matcher.
> - **Sistema de Mensajes Integrado**: Las intenciones ahora renderizan contenido directamente en el nodo.
</details>

<details>
<summary><strong>V-0.2.3 (Noviembre 2025) - Analítica y STT</strong></summary>

### 📊 Monitoreo
> - Dashboard operativo de **Grafana y Prometheus** integrado.
> - Historial conversacional completo con **Análisis de Sentimientos**.
> - Inserción de logs de Speech-to-Text (STT).
</details>

<details>
<summary><strong>V-0.2.2 (Octubre 2025) - Generación de IA por Fases</strong></summary>

### 🤖 Reforestación y Resaltado
> - **Pipeline de IA de 6 fases**: Análisis conversacional, estructuración semántica, sinónimos y aplicación (Groq Models).
> - **Modo Oscuro**: Implementación nativa y caché inteligente.
> - Resaltado visual interactivo (`Highlight`) de entidades reconocidas en los cuadros de texto.
</details>

<details>
<summary><strong>V-0.2.1 (Septiembre 2025) - Base Generativa</strong></summary>

### 🧠 Modelos Fundacionales
> - Modificación de endpoints para modelos NER (Named Entity Recognition).
> - Refactorización de Prompts base del sistema generativo.
</details>

<details>
<summary><strong>V-0.2.0 (Agosto 2025) - IA Multimodal y Web Widgets</strong></summary>

### 🎨 Diseño y Widget
> - **Widget Web V2**: Lanzamiento del Widget Web interactivo y embebible (Shadow DOM).
> - Backend Generativo asilado y Panel de Control de usuarios.
</details>

<details>
<summary><strong>V-0.1.0 (Julio 2025) - Workspaces y Fertilizantes</strong></summary>

### 🏢 Estructura Multi-Entorno
> - Lanzamiento oficial del sistema de **Workspaces** (Cuentas independientes y segregación de usuarios).
> - Implementación del modal interactivo de Testing en Vivo para **Webhooks**.
</details>

<details>
<summary><strong>V-0.0.2 (Junio 2025) - Metadatos Avanzados</strong></summary>

### ⚙️ Parámetros y Contextos
> - Manejo global de Metadatos de usuario (sys variables) y contextos funcionales de entrada y salida.
> - Requerimiento lógico de parámetros del usuario para validación.
</details>

<details>
<summary><strong>V-0.0.1 (Mayo 2025) - Lanzamiento Inicial</strong></summary>

### 🌱 La Semilla de TreeFlow
> - Base arquitectónica de Trees, Branches y Leaves.
> - Implementación nativa de entidades `@sys` base y detectores lógicos con FastApi + React.
> - Ajustes de Umbrales Machine Learning vs Fuzzy Logic.
</details>
