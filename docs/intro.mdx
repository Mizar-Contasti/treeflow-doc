---
sidebar_position: 1
---

# Empieza Aqui

**Treeflow** es una WebApp para Desarrollar Chatbots Deterministas (Trees), enfocados en la efectividad al bajo costo, gracias a modelos generativos. Esto nos permite usar las ventajas de los dos mundos: control de deterministas y automatización de generativos.

## Proyecto en Fase Beta

Te damos la bienvenida a Treeflow, un proyecto que actualmente se encuentra en **fase beta**. Estamos trabajando continuamente para mejorar la plataforma y añadir nuevas funcionalidades.

### Acceso a la Plataforma

- **Dominio**: [tf.botsmexico.com](https://tf.botsmexico.com)
- **Cuenta predeterminada**: 
  - Usuario: `admin`
  - Contraseña: `admin`

Con esta cuenta podrás acceder y probar todas las características disponibles en la plataforma.

### Lo que Necesitarás
- Navegador web moderno (Chrome, Firefox, Edge o Safari)

## Generate a new site

Generate a new Docusaurus site using the **classic template**.

The classic template will automatically be added to your project after you run the command:

```bash
npm init docusaurus@latest my-website classic
```

You can type this command into Command Prompt, Powershell, Terminal, or any other integrated terminal of your code editor.

The command also installs all necessary dependencies you need to run Docusaurus.

## Start your site

Run the development server:

```bash
cd my-website
npm run start
```

The `cd` command changes the directory you're working with. In order to work with your newly created Docusaurus site, you'll need to navigate the terminal there.

The `npm run start` command builds your website locally and serves it through a development server, ready for you to view at http://localhost:3000/.

Open `docs/intro.md` (this page) and edit some lines: the site **reloads automatically** and displays your changes.
